# Patched twikit fork: fixes "Couldn't get KEY_BYTE indices" /
# "'ClientTransaction' object has no attribute 'key'" caused by X's changed
# webpack chunk format (upstream twikit 2.3.3 is broken as of Jul 2026).
# See https://github.com/d60/twikit/pull/432 — switch back to `twikit>=2.0.0`
# once the fix is merged upstream.
twikit @ git+https://github.com/leslienwu/twikit-patched.git@fix/ondemand-key-byte-indices-2026
feedparser>=6.0.11
PyYAML>=6.0.2
python-dotenv>=1.0.1
anthropic>=0.40.0
openai>=1.50.0
python-dateutil>=2.9.0
