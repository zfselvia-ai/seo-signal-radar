# twikit removed: X broke client-transaction signing (ondemand.s chunk gone),
# so every twikit request fails. X tweets are now fetched via Nitter RSS in
# seodigest/x_source.py — no auth, no cookies, more stable.
feedparser>=6.0.11
PyYAML>=6.0.2
python-dotenv>=1.0.1
anthropic>=0.40.0
openai>=1.50.0
python-dateutil>=2.9.0
